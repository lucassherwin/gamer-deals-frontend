import React, { Component } from 'react'

export class Account extends Component {
    render() {
        return (
            <div>
                <h1>User account page</h1>
                <h2>Wishlist</h2>
            </div>
        )
    }
}

export default Account


// this is users account if they are logged in
// this will show user info: username, profile pic, etc
// show wishlist/favorites